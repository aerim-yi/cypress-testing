it('visit GU website', () => {
    cy.visit('/')
    cy.get('cerberus-navbar-main-menu-item gu-simple-text').contains('expansion').click()
})

it('expansion'), () => {
    it('How to use env varibales'), () => {
        const trial = Cypress.env('POWERFUL CHAMPIONS, PRIZE DRAWS @ NEW WAYS TO PLAY');
    }
}
it('expansion'), () => {
    it('How to use env varibales'), () => {
        const divine = Cypress.env('PLAY GODS UNCHAINED'); (SOLD);
    }
}
it('expansion'), () => {
    it('How to use env varibales'), () => {
        const mortal = Cypress.env('PLAY GODS UNCHAINED'); ('BUY CARD PACKS');
    }
}